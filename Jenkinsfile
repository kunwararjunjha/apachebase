pipeline {
    agent any
    stages {
        stage('build maven') {
            parallel {
                            stage('Build Docker Image') {
            steps {
                        script {
                          def dockerImage = docker.build('httpd', '.')

                }
    }
}


}
}
}
}
